Alla registrazione vengono chieste solo le informazioni principali(email,password,nome,cognome,data di nascita,stato,sesso (come facebook))

create table Utente(
	ID serial primary key,
	email varchar(25) not null unique,
	password char(32) not null
)

insert into Utente(email,password) values ('pippo@gmail.com','pippo')
insert into Utente(email,password) values ('pluto@gmail.com','pluto')

create table Persona(
	email varchar(25) primary key,
	username varchar(25) not null unique,
	nome varchar(25) not null,
	cognome char(32) not null,
	sesso char(1) not null,
	dataNascita date not null,
	squadra varchar(10) not null,
	foreign key(email) references Utente(email),
	check(sesso = 'M' or sesso = 'F')
)

insert into Persona(email,username,nome,cognome,sesso,dataNascita) values('pippo@gmail.com','pippo','pippo','de pippis','M','1920-10-08')

create table Offerta(
	ID serial primary key,
	titolo varchar(30) not null,
	ospitante varchar(25) not null references Persona(username),
	ospitato varchar(25) not null references Persona(username),
	recensioneFattaDaOspitante integer references Recensione,
	recensioneFattaDaOspitato integer references Recensione
)

create table Partita(
	homeTeam varchar

create table Recensione(
	ID serial primary key,
	numStelle integer not null default 0,
	descrizione varchar(100)
)

insert into Utente values(default,'giovanni@gmail.com','aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa');
insert into Persona values('giovanni@gmail.com','giovanni','giovanni','storti','M','1920-10-08','Inter');
insert into Utente values(default,'giacomo@gmail.com','aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa');
insert into Persona values('giacomo@gmail.com','giacomo','giacomo','poretti','M','1920-10-08','Milan');
insert into Utente values(default,'aldo@gmail.com','aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa');
insert into Persona values('aldo@gmail.com','aldo','aldo','baglio','M','1920-10-08','Catania');
insert into Offerta values(default,'pippo','giovanni','giacomo');
insert into Offerta values(default,'pluto','giacomo','aldo');
insert into Offerta values(default,'paperino','aldo','giovanni');

	